/* eslint-disable react/prop-types */
const HeaderTitle = ({ title }) => {
  return (
    <div className="mb-2 mt-14 text-center text-3xl font-bold text-[var(--color)]">
      {title}
    </div>
  );
};

export default HeaderTitle;
